import * as React from 'react';

const HorizontalRule = () => {
  return (
    <hr
      style={{
        width: '70%',
        margin: 'auto',
      }}
    />
  );
};

export default HorizontalRule;
