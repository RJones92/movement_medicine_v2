import * as React from 'react';

const HorizontalRule = () => {
  return (
    <hr
      style={{
        width: '70%',
        margin: 'auto',
        borderTop: '3px solid',
      }}
    />
  );
};

export default HorizontalRule;
